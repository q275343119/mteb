- name: Check HF_TOKEN exists
  run: |
    if [ -z "${{ secrets.HF_TOKEN }}" ]; then
      echo "HF_TOKEN is missing!"
      exit 1
    else
      echo "HF_TOKEN is set (length: ${#{{ secrets.HF_TOKEN }}})"
    fi
